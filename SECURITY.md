# Security and Privacy

This repository is intended for research and demonstration only.

Do not commit protected health information, DICOM files, raw CT data, patient identifiers, private reports, large model outputs, or credentials.

Before publishing to GitHub, check:

```powershell
git status --short
```

The repository `.gitignore` excludes common local data folders and generated artifacts, but final responsibility for reviewing committed files remains with the maintainer.

